#!/usr/bin/env sh

set -e

echo "🔍 Running lint-staged (pre-commit)..."
pnpm exec lint-staged

echo "🔍 Running TypeScript check (pnpm check-types)..."
pnpm check-types

echo "✅ Pre-commit checks passed!"
